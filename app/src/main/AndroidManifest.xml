<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="de.mobcomp.grades">

    <uses-permission android:name="ANDROID.PERMISSION.INTERNET" />

    <application android:allowBackup="true" android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher" android:theme="@style/AppTheme">

        <!-- Content Provider -->
        <provider
            android:authorities="de.mobcomp.grades.provider"
            android:name=".database.GradesProvider"
            android:exported="false" />
    </application>

</manifest>
